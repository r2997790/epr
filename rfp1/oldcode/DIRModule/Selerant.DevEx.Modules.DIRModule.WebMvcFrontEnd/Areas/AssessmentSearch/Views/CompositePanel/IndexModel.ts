namespace DX.DIRModule.AssessmentSearch
{
    export class CompositePanelIndex extends DX.Mvc.SearchPanelBase
    {
        static $$className = 'DX.DIRModule.AssessmentSearch.CompositePanelIndex';

        constructor(element: HTMLElement)
        {
            super(element);
        }

        __applyJSONData()
        {
            super.__applyJSONData();
        }

        protected _initialize()
        {
            super._initialize();
        }

        protected _initializePanel()
        {
            
        }
    }
}