namespace DX.DIRModule.AssessmentSearch
{
    export class AssessmentSearchRecentPanel extends DX.Mvc.RecentSearchPanel
    {
        static $$className = 'DX.DIRModule.AssessmentSearch.AssessmentSearchRecentPanel';

        constructor(element: HTMLElement)
        {
            super(element);
        }
    }
}