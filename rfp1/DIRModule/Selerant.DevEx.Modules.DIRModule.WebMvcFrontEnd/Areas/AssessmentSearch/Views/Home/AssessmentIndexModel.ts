namespace DX.DIRModule.AssessmentSearch
{
    export class Index extends DX.Mvc.SearchBase
    {
        static $$className = 'DX.DIRModule.AssessmentSearch.Index';

        constructor(element: HTMLElement)
        {
            super(element);
        }

        __applyJSONData()
        {
            super.__applyJSONData();
        }

        protected _initialize()
        {
            super._initialize();
        }
    }
}