namespace DX.DIRModule.AssessmentNavigator.Shared.Tabs
{
	export class BusinessCostsGridTab extends DX.Mvc.ViewControl
	{
		static $$className = "DX.DIRModule.AssessmentNavigator.Shared.Tabs.BusinessCostsGridTab";

		constructor(element: HTMLElement)
		{
			super(element);
		}

		public __applyJSONData()
		{
			// (OVERRIDE)
			super.__applyJSONData();

		}

		protected _initialize(): void
		{
			// (OVERRIDE)
			super._initialize();
		}

	}
}